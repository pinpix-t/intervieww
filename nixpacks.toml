# Nixpacks configuration for Railway
# Explicitly tells Railway this is a Python project

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd talking && python listener.py"

